namespace TestAp.Reservation
{
    internal class TravelAgent
    {
        //public void ConfirmPackage(Person customer) 
        //{
        //    bool create = customer.CreateResIfNull();

        //    foreach (DateTime d in customer.TravelDates)
        //    {
        //        ConfirmationNumber  cn = Reservation.ConfirmReservation(customer.Name, d, create)
        //    }

        //    if (cn == null && create = true)
        //    {
        //        Reservation res = new Reservation(name, d);
        //    }

        //    if (cn != null)
        //    {
        //        customer.AddConfirmation(cn);
        //    } // the design should be easy to undestand and easy to extens and to change
        //}
    }
}
